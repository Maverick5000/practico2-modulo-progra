<%= semantic_form_for @rented do |f| %>
    <%= f.inputs do %>
      <%= f.input :client, :as => :select %>
      <%= f.input :tape, :as => :select, :collection => Tape.where(available: true) %>
    <% end %>
    <%= f.actions do %>
      <%= f.action :submit, :as => :button %>
      <%= f.action :cancel, :as => :link %>
    <% end %>
  <% end %>